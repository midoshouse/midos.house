query SetQuery($set_id: ID!) {
    set(id: $set_id) {
        fullRoundText
        phaseGroup {
            phase {
                event {
                    slug
                }
                name
            }
        }
        slots {
            entrant {
                name
            }
        }
    }
}
