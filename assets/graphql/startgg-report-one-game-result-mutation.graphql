mutation ReportOneGameResultMutation($setID: ID!, $winnerEntrantID: ID!) {
  reportBracketSet(setId: $setID, winnerId: $winnerEntrantID, gameData: [
    {gameNum: 1, winnerId: $winnerEntrantID},
  ]) {
    id
  }
}
